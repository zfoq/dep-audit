## Summary

<!-- What does this PR do and why? -->

## Changes

-

## Test plan

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check src/ tests/` passes

<!-- If this adds or modifies DB entries, also check: -->
- [ ] DB entries have `type`, `replacement`, `reason`, and `confidence` filled in
- [ ] `dep-audit db list` shows the new entries
