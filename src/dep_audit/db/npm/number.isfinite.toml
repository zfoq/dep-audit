name = "number.isfinite"
ecosystem = "npm"
type = "stdlib_backport"
confidence = 0.95

replacement = "Number.isFinite()"
stdlib_since = "1.0"

flags = [
    "stdlib_backport: Number.isFinite() available since Node 1 / ES2015",
]

validated = 2026-03-07
