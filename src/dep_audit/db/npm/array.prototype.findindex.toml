name = "array.prototype.findindex"
ecosystem = "npm"
type = "stdlib_backport"
confidence = 0.95

replacement = "Array.prototype.findIndex()"
stdlib_since = "4.0"

flags = [
    "stdlib_backport: Array.prototype.findIndex() available since Node 4 / ES2015",
]

validated = 2026-03-07
