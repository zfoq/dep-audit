name = "array.prototype.find"
ecosystem = "npm"
type = "stdlib_backport"
confidence = 0.95

replacement = "Array.prototype.find()"
stdlib_since = "4.0"

flags = [
    "stdlib_backport: Array.prototype.find() available since Node 4 / ES2015",
]

validated = 2026-03-07
