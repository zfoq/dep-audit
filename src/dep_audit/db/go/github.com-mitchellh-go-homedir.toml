name = "github.com/mitchellh/go-homedir"
ecosystem = "go"
type = "stdlib_backport"
confidence = 0.90

replacement = "os.UserHomeDir() (stdlib since Go 1.12)"
stdlib_since = "1.12"

flags = [
    "stdlib_backport: os.UserHomeDir() was added in Go 1.12",
    "stdlib_backport: go-homedir is unmaintained since 2019",
    "stdlib_backport: drop the dependency and call os.UserHomeDir() directly",
]

validated = 2026-03-07
