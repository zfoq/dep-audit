name = "go.uber.org/multierr"
ecosystem = "go"
type = "stdlib_backport"
confidence = 0.75

replacement = "errors.Join (stdlib since Go 1.20)"
stdlib_since = "1.20"

flags = [
    "stdlib_backport: errors.Join was added in Go 1.20 and covers the primary use case",
    "stdlib_backport: for simple error combining, errors.Join is a drop-in replacement",
    "note: multierr.Combine has slightly different nil-handling semantics; verify before removing",
]

validated = 2026-03-07
